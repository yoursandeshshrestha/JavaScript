// slice(): Extracts a section of the array and returns a new array.

let fruits = ["Mango", "Orange", "Banana", "Kiwi"]

let slicedFruits = fruits.slice(1, 3); // one is starting and 2 is end
console.log(slicedFruits); // Output: ["Orange", "Banana"]
